#include <stdio.h>

int main() {
  const int BIRTHYEAR = 1990;
  float x = 5;
  float y = 2;
  float sum = (float) x / y;

  printf("%.2f\n", sum); // 2.50
  printf("Birthyear: %d", BIRTHYEAR);
  int mySum = x + y;
  print("sum is: %f", mySum);
  
  return 0;
}
